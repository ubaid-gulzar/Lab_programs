// program ro check whateher a number is larger or not using ternory
#include<stdio.h>
int main(){
    int x,y; 
    printf("enter the first number");
    scanf("%d",&x);
    printf("enter the second number");
    scanf("%d",&y);
    if (x>y);
    printf("%d is a grater",x);
     if (y>x);
     printf("%d is a larger number", y);
       if (x=y);
       printf("%d is equal to %d", x,y);
        return 0;
}